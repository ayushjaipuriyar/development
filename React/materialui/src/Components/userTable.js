import React from "react";
import { Table } from "@material-ui/core";
export default function userTable(records, headCells) {
  const TableContainer = (props) => {
    <Table></Table>;
  };
  return;
}
